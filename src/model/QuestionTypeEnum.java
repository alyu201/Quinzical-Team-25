package model;

/**
 * QuestionTypeEnum contains the types of game mode the game can have and allows the game 
 * to keep track of the state it is in and change game flow depending on the 
 * game mode state.
 */
public class QuestionTypeEnum {
	public enum QuestionType {
		INTERNATIONAL,
		NEWZEALAND
	}
}
